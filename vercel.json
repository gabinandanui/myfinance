{
  "buildCommand": "cd fin && npm install && npm run build",
  "outputDirectory": "fin/dist",
  "framework": null,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "fin/dist/index.html"
    }
  ]
}
